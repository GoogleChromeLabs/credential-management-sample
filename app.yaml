application: credential-management-sample
version: 1
runtime: python27
api_version: 1
threadsafe: true

libraries:
- name: ssl
  version: latest
- name: pycrypto
  version: latest

handlers:
- url: .*
  script: main.app
  secure: always
